# Targeted durable-store tests

Build `test_targeted_recovery_store` through the normal CMake build. It uses
the production journal implementation and synthetic temporary data only.
See [the focused test guide](README-operator-recovery-tests.md) for commands,
coverage and limitations.
