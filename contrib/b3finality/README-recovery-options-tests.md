# Recovery manifest and startup-option tests

Build `test_finality_recovery_options` through the normal CMake build. It
checks strict parsing and bounded synthetic files without opening a wallet or
journal. See [the focused test guide](README-operator-recovery-tests.md).
